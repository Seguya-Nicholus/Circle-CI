const mark = require("../index");

// test('Testing the Mark is a Number', ()=>{
//     expect(typeof(mark.marks)).toBe('number');
// })

test("Testing the Mark is a Number", () => {
  expect(mark.isValidate()).toBeTruthy();
});

test("Testing the Mark is a Number", () => {
  expect(mark.isValidate()).toBeTruthy();
});
